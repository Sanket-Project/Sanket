import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';

export default tseslint.config(
  { ignores: ['dist'] },
  {
    extends: [
      js.configs.recommended,
      ...tseslint.configs.recommended,
    ],
    files: ['**/*.{ts,tsx}'],
    languageOptions: {
      ecmaVersion: 2020,
    },
    plugins: {
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': [
        'warn',
        { allowConstantExport: true },
      ],
      // Incremental adoption: `any` usage is surfaced as a warning (tracked as
      // type debt to burn down) rather than blocking CI. Genuine errors —
      // unused code, prefer-const, undefined vars — still fail the lint gate.
      '@typescript-eslint/no-explicit-any': 'warn',
      // Allow intentionally-unused, underscore-prefixed identifiers (e.g. `_source`,
      // destructured throwaways) — a standard convention that keeps the rule useful
      // for real dead code without false positives.
      '@typescript-eslint/no-unused-vars': [
        'error',
        {
          argsIgnorePattern: '^_',
          varsIgnorePattern: '^_',
          caughtErrorsIgnorePattern: '^_',
        },
      ],
    },
  }
);

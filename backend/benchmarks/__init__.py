"""Runnable performance benchmarks for SANKET backend optimizations.

These are *measurement* scripts, not pytest tests — run them directly against a
reachable Postgres / Redis to capture before/after numbers. See README.md.
"""

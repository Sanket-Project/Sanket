"""Offline, dependency-light forecasting benchmarks.

Runs SANKET's real walk-forward backtest + metrics against naive baselines on
synthetic-but-realistic demand, with no live database and no heavy model deps
(torch/statsforecast). See README.md.
"""
